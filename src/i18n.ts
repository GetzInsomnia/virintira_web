// Configure supported locales here. Add more codes as needed, e.g. 'zh', 'ja'.
export const locales = ['th', 'en']
export const defaultLocale = 'th'
// Prefix default locale in paths only when necessary
export const localePrefix = 'as-needed'
